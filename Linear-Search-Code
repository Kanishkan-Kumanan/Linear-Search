#code to locate card which is sorted in decreasing order in linear method
#Time Complexity is O(N) where N is number of cards.


def linear_search(cards,query):
    position = 0
    
    while position < len(cards):
        
        if cards[position] == query:
            return position
        
        position+=1
   
    return -1    
            
cards = [] 
query = 4
p = linear_search(cards,query)

#you can try it with various test_cases(various cards, various queries)
